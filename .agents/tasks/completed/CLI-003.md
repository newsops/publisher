# CLI-003: Admin API Client Package

- **Status**: in-progress
- **Created**: 2026-09-13
- **Branch**: main
- **Scope**: packages/admin-client, packages/ops-cli, documentation, harness

## Objective

Separate the reusable authenticated Admin API client from command-line parsing
so the official CLI and future agents share one provider-neutral contract.

## Plan

- [x] TC-01: Create and typecheck explicit client configuration and exports.
- [x] TC-02: Test request construction through injected transport.
- [x] TC-03: Test safe normalized error behavior and redaction.
- [x] TC-04: Refactor existing CLI commands to use the client with compatible
      machine-readable envelopes.
- [x] TC-05: Add source-boundary enforcement and package documentation.
- [x] TC-06: Run focused and workspace regression verification.

## Progress

- Added the dependency-free `@publisher/admin-client` workspace package.
- Moved CLI HTTP calls to the package while retaining CLI configuration and
  JSON-envelope ownership in `@publisher/ops-cli`.
- Added injected-transport unit coverage and static dependency-boundary checks.
- Focused typecheck/test, the existing CLI local-server contract, and the
  repository harness scan passed.
- `pnpm typecheck`, `pnpm test`, `pnpm build`, and `pnpm -r lint` passed on
  2026-09-13. The full harness result was 34 files / 145 tests passed.

## Blockers

- None.
