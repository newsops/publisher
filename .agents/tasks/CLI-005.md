# CLI-005: Agent plugin lifecycle operations

- **Status**: in-progress
- **Created**: 2026-09-17
- **Branch**: pending implementation branch
- **Scope**: packages/ops-cli, packages/admin-client, scripts/harness, docs

## Objective

Expose reviewed, site-scoped plugin lifecycle operations through the agent CLI
without a browser, direct database access, direct object storage access, or
implicit public release.

## Plan

- [ ] TC-01: Implement authenticated, site-qualified `plugin list` and
      `plugin get` CLI reads through the Admin API.
- [ ] TC-02: Implement revision-checked `plugin configure` from a parsed JSON
      input file and reject missing local preconditions before a mutation request.
- [ ] TC-03: Implement revision-checked `plugin validate`, `plugin enable`,
      and `plugin disable` actions; reject unknown verbs without a request.
- [ ] TC-04: Keep CLI envelopes token-safe and document the GA4
      consent-denied configuration boundary with no implicit enable or release.
- [ ] TC-05: Run the focused plugin contracts, workspace typecheck, full test
      suite, and harness scan successfully.

## Progress

### 2026-09-17

- GATE-WRITE and delegated GATE-APPROVAL passed.
- GATE-IMPLEMENT created this task record before plugin-lifecycle code work.

## Decisions

- Use only the authenticated, site-scoped Admin API through the existing
  client boundary. The CLI must not bypass authorization, auditing, validation,
  revisions, or provider neutrality.
- Treat configuration, validation, enablement, and public release as distinct
  operations. Google Analytics stays consent-denied until an approved consent
  signal exists.

## Blockers

- None for implementation. Live configuration requires an authenticated CLI
  session; enablement and release retain their separate production safeguards.

## Result

Pending implementation.
