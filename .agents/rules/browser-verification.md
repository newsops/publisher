# Browser verification

After UI changes, verify at minimum the home page, one article page, one category page, search shell, mobile width, and an unknown route. Check console errors, broken links, keyboard focus, and layout overflow.
